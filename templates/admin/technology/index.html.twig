{% extends 'admin/base.html.twig' %}

{% block title %}Liste des technologies{% endblock %}

{% block body %}
    <ol class="breadcrumb breadcrumb-sm">
        <li class="breadcrumb-item">
            <a href="{{ path('admin_dashboard') }}">Dashboard</a>
        </li>
        <li class="breadcrumb-item active">Liste des technologies</li>
    </ol>

    {% include 'partials/_alert.html.twig' %}

    <!-- Table Technology -->
    <div class="card mb-3">
        <div class="card-header">
            <i class="fas fa-table"></i>
            Toutes les technologies
        </div>
        <div class="card-body card-body-sm">
            <div class="table-responsive">
                <table class="table table-striped table-sm" id="dataTable" width="100%" cellspacing="0">
                    <thead class="text-center">
                    <tr>
                        <th>#</th>
                        <th>Nom</th>
                        <th>Slug</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tfoot class="text-center">
                    <tr>
                        <th>#</th>
                        <th>Nom</th>
                        <th>Slug</th>
                        <th>Action</th>
                    </tr>
                    </tfoot>
                    <tbody class="text-center">
                    {% for techno in technos %}
                        <tr>
                            <td>{{ techno.id }}</td>
                            <td>{{ techno.name }}</td>
                            <td>{{ techno.slug }}</td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a href="{{ path('admin_technology_update', {'slug': techno.slug}) }}" class="btn btn-md btn-secondary" title="Modifier la technologie">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a href="{{ path('admin_technology_delete', {'id': techno.id}) }}" class="btn btn-md btn-danger delete-link" title="Supprimer la technologie">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}